{
  "name": "sentrook",
  "private": true,
  "description": "JS workspace root — OpenClaw plugin only. Python packages are not npm workspaces.",
  "workspaces": [
    "integrations/openclaw/plugin"
  ],
  "scripts": {
    "changeset": "changeset",
    "version": "changeset version && npm install --package-lock-only"
  },
  "devDependencies": {
    "@changesets/cli": "3.0.0",
    "@types/node": "26.2.0"
  }
}
