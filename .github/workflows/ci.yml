# Phase 1 public-ready CI for Sentrook.
# Does NOT check out Rookery, run sanitize-gate, or run test-engine.
name: ci

on:
  pull_request:
  push:
    branches: [main]
    tags: ["*"]

permissions:
  contents: read

concurrency:
  group: sentrook-ci-${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

env:
  PYTHON_VERSION: "3.12"
  NODE_VERSION: "22"

jobs:
  lint-python:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Install uv
        uses: astral-sh/setup-uv@20cfd1bf945f4377ade1205e4dbc17946fc9a30d # v10.0.1
        with:
          enable-cache: true

      - name: Set up Python
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      - name: Install (dev)
        run: |
          uv venv .venv
          source .venv/bin/activate
          make install-dev

      - name: Lint
        run: make lint

  test-sentrook:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Install uv
        uses: astral-sh/setup-uv@20cfd1bf945f4377ade1205e4dbc17946fc9a30d # v10.0.1
        with:
          enable-cache: true

      - name: Set up Python
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      - name: Install (dev)
        run: |
          uv venv .venv
          source .venv/bin/activate
          make install-dev

      - name: Unit tests (sentrook)
        run: make test-sentrook

  test-testnest-unit:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Install uv
        uses: astral-sh/setup-uv@20cfd1bf945f4377ade1205e4dbc17946fc9a30d # v10.0.1
        with:
          enable-cache: true

      - name: Set up Python
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      - name: Install (dev)
        run: |
          uv venv .venv
          source .venv/bin/activate
          make install-dev

      - name: Unit tests (testnest harness)
        run: make test-testnest

  testnest-smoke:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Install uv
        uses: astral-sh/setup-uv@20cfd1bf945f4377ade1205e4dbc17946fc9a30d # v10.0.1
        with:
          enable-cache: true

      - name: Set up Python
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      - name: Install (dev)
        run: |
          uv venv .venv
          source .venv/bin/activate
          make install-dev

      - name: DEMO smoke TestNest
        run: make smoke

  test-plugin:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Set up Node
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: npm
          cache-dependency-path: package-lock.json

      - name: Install JS workspace
        run: npm ci

      - name: Plugin tests + pack check
        run: make plugin-test

  notify-rookery:
    # Push to main / tags only — non-blocking private full-eval visibility.
    # PRs merge on Sentrook CI; full-eval is a release gate (manual dispatch OK).
    if: ${{ github.event_name == 'push' && (github.ref == 'refs/heads/main' || startsWith(github.ref, 'refs/tags/')) }}
    runs-on: ubuntu-latest
    needs:
      - lint-python
      - test-sentrook
      - test-testnest-unit
      - testnest-smoke
      - test-plugin
    permissions:
      contents: read
    steps:
      - name: Dispatch Rookery full-eval
        env:
          ROOKERY_DISPATCH_TOKEN: ${{ secrets.ROOKERY_DISPATCH_TOKEN }}
          SENTROOK_SHA: ${{ github.sha }}
        run: |
          set -euo pipefail
          if [ -z "${ROOKERY_DISPATCH_TOKEN}" ]; then
            echo "ROOKERY_DISPATCH_TOKEN unset — dispatch skipped (secret not provisioned)"
            exit 0
          fi
          curl -sS -X POST \
            -H "Authorization: Bearer ${ROOKERY_DISPATCH_TOKEN}" \
            -H "Accept: application/vnd.github+json" \
            -H "X-GitHub-Api-Version: 2022-11-28" \
            "https://api.github.com/repos/FirstDataUnion/Rookery/dispatches" \
            -d "{\"event_type\":\"sentrook-eval\",\"client_payload\":{\"sentrook_sha\":\"${SENTROOK_SHA}\"}}"
          echo "Dispatched sentrook-eval for ${SENTROOK_SHA}"
